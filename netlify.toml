[build]
  command = "npm run build"
  publish = "out"

# Redirect non-www to www (prevents duplicate content in search engines)
[[redirects]]
  from = "https://mapleridgeconstruction.com/*"
  to = "https://www.mapleridgeconstruction.com/:splat"
  status = 301
  force = true

[[plugins]]
  package = "@netlify/plugin-nextjs"
