---
import BaseLayout from '../../../layouts/BaseLayout.astro'
import PageHero from '../../../components/PageHero.astro'
import CTASection from '../../../components/CTASection.astro'
import Breadcrumbs from '../../../components/Breadcrumbs.astro'
import { serviceAreas, serviceAreasBySlug } from '../../../data/service-areas'
import { services } from '../../../data/services'
import { contact } from '../../../data/contact'

export function getStaticPaths() {
  return serviceAreas.map((area) => ({
    params: { slug: area.slug },
    props: { area },
  }))
}

const { area } = Astro.props
const { slug } = Astro.params

const otherAreas = serviceAreas.filter((a) => a.slug !== slug).slice(0, 4)

const title = `${area.heroTitle} | Maple Ridge Construction`
const description = `${area.description} Call ${contact.phone} for a free estimate.`

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: area.faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: { '@type': 'Answer', text: faq.answer },
  })),
}
---

<BaseLayout
  title={title}
  description={description}
 
>
  <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} />

  <PageHero
    label={`${area.county} County, Tennessee`}
    title={area.heroTitle}
    description={area.description}
  />
  <Breadcrumbs items={[
    { name: 'Home', href: '/' },
    { name: 'Service Areas', href: '/service-areas' },
    { name: `${area.name}, TN` },
  ]} />

  <section class="py-20 lg:py-28">
    <div class="container-wide section-padding">
      <div class="grid lg:grid-cols-2 gap-16 items-start">
        <div>
          <span class="section-label">Our services in {area.name}</span>
          <h2 class="section-title">Construction services in {area.county} County</h2>
          <div class="mt-6 space-y-4 text-stone-600 leading-relaxed">
            <p>{area.localContent}</p>
          </div>
        </div>

        <div>
          {services.map((service, index) => (
            <a
              href={`/services/${service.slug}`}
              class="group flex gap-6 border-t border-stone-300 py-6 last:pb-0"
            >
              <span class="font-display text-lg text-stone-400 flex-shrink-0 w-8 pt-0.5" aria-hidden="true">0{index + 1}</span>
              <div>
                <h3 class="font-display text-xl font-semibold text-stone-900 mb-1 group-hover:text-ridge-800 transition-colors">
                  {service.name}
                </h3>
                <p class="text-stone-600 text-sm leading-relaxed mb-3">{service.description}</p>
                <span class="text-link text-sm">Learn more</span>
              </div>
            </a>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 lg:py-28 bg-white">
    <div class="container-wide section-padding">
      <div class="text-center max-w-2xl mx-auto mb-12">
        <span class="section-label">Working in {area.name}</span>
        <h2 class="section-title">Common questions</h2>
      </div>

      <div class="max-w-3xl mx-auto">
        {area.faqs.map((faq) => (
          <div class="border-b border-stone-200 py-6 first:pt-0 last:border-0">
            <h3 class="font-semibold text-stone-900 mb-3">{faq.question}</h3>
            <p class="text-stone-600 leading-relaxed">{faq.answer}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="py-16 lg:py-20 border-t border-stone-200">
    <div class="container-wide section-padding">
      <div class="text-center max-w-2xl mx-auto mb-12">
        <span class="section-label">More service areas</span>
        <h2 class="section-title">Also serving Southeast Tennessee</h2>
      </div>

      <div class="grid grid-cols-2 lg:grid-cols-4 gap-x-10 gap-y-10 max-w-4xl mx-auto">
        {otherAreas.map((otherArea) => (
          <a href={`/service-areas/${otherArea.slug}`} class="group block border-t border-stone-300 pt-4">
            <h3 class="font-display text-lg font-semibold text-stone-900 group-hover:text-ridge-800 transition-colors">
              {otherArea.name}
            </h3>
            <p class="text-sm text-stone-500">{otherArea.county} County</p>
          </a>
        ))}
      </div>

      <p class="text-center text-stone-500 mt-10">
        <a href="/service-areas" class="text-link">View all service areas</a>{' '}
        or <a href="/contact" class="text-link">contact us</a> to see if we serve your area.
      </p>
    </div>
  </section>

  <CTASection
    heading={`Working in ${area.name}? Let's talk.`}
    description={`Call or send a note about your project. We'll come look at it and tell you what it'll take.`}
  />
</BaseLayout>
